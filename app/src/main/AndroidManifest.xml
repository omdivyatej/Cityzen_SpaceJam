<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.cityzen_spacejam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cityzen_SpaceJam">
        <activity android:name=".MainActivity"></activity>
        <activity android:name=".ForumActivity" />
        <activity android:name=".BookRestaurant" />
        <activity android:name=".localrefresh" />
        <activity android:name=".Hospitals" />
        <activity android:name=".placestovisit" />
        <activity android:name=".top10" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".AddAnswer"/>
        <activity android:name=".ForumAnswer"/>
        <activity android:name=".LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>